/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            'images.pexels.com',
            'api.dicebear.com',
        ]
    }
};

export default nextConfig;
