/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            'images.pexels.com',
            'api.dicebear.com',
            "lh3.googleusercontent.com",
        ]
    }
};

export default nextConfig;
